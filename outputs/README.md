# Output availability

This GitHub package contains compact reproducibility outputs and normalized curves. Raw curves and complete per-seed GP artifacts are provided in the corresponding Zenodo package.

The 46 GB stack-array cache and source microscopy images are not included. Rebuild the cache from authorized images with Stage 01.
